<!DOCTYPE html>
<html>
  <head>
    <meta charset='utf-8'>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title>Fantempo</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body class="landing d-flex flex-column justify-content-between align-items-center">
    <header class="container">
      <div>fantempo</div>
      <%= render 'shared/notifications' %>
    </header>
    <section class="container">
      <div class="d-flex justify-content-center">
        <div class="row">
          <div class="col-sm-12 col-10">
            <%= yield %>
          </div>
        </div>
      </div>
    </section>
    <footer class="container">
      <div class="d-flex justify-content-xl-end justify-content-lg-end justify-content-center">
        <span>Todos los derechos reservados © Fantempo</span>
      </div>
    </footer>
  </body>
</html>